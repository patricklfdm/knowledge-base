# H3-001C：从页面到表单数据

## 目标与事实

让读者从 HTML/CSS/DOM 与事件，走到把文字输入转换为合法行程对象。基线 HEAD 54a6619，v5 工作区干净，origin/v5 db02102，本地已有三个待发布提交。H0/H1 已验收，不重新初始化。Node 24.21.0/npm 11.19.0；Python 3.13.0 仅作为本地静态文件服务。以 BACKLOG 为唯一台账。

## 范围与步骤

1. 最小协调用户当前覆盖：内容优先、停止浏览器、每批验证后自动 push，直到另行说明；保留历史证据与全部 CI。
2. 新增 F07A 页面与 DOM、F07B 表单两篇，独立 examples/web-forms（无 npm 依赖），正常/边界/失败测试。教学 CSS 不改知识库表现层。
3. 复核 MDN 官方语义，完成作者自审后再做一次按读者任务的正文复核。更新必要导航。
4. 隔离验证现有 kb:verify 与 npm test、锁文件漂移；不运行任何浏览器。静态 HTTP 服务只做文本请求并清理。
5. 更新报告/BACKLOG/STATE，小提交并正常 push v5；跟踪同 SHA CI/Pages 与线上 HTTP 文本冒烟。

## 验证与边界

表单纯函数测试含空白、类型、整数/范围、错误格式、HTML 样式文字及不修改输入；故意未捕获非法输入以非零退出。DOM 绑定代码仅语法/源码复核，不声称点击/Enter/焦点/屏幕阅读器已测试。G7 和应用交互 NOT_RUN：用户批准移至集中验收阶段，关联 H4-UI-SITE/H4-UI-APP。

复用本会话已干净安装的隔离副本（/tmp/kb-h3b-path.txt），受控同步当前文件，记录锁摘要；不新增站点依赖或改 Quartz/CSS。新示例单独复制到含空格目录验证无需根依赖。

## Progress

- [x] 仓库核对、参考资料与用户授权区分，协调执行规则。
- [x] 两篇正文与独立例子、测试。
- [x] 来源/两次编辑复核、隔离非浏览器验收。
- [x] 报告/台账/恢复点、提交推送和同 SHA 部署。

## Decisions and discoveries

F07 拆为两篇，先学 DOM 再学 FormData；表单层显式转换文字，F06 的业务对象类型边界保持不变。浏览器代码用 JavaScript，避免新增 TypeScript 浏览器打包器。复用 Python 标准静态服务器，不自建服务框架。样例只是本地预览，不声称持久化或 API 已完成。

第二遍复核修正路线表格、04-frontend目录和status区不主动聚焦的语义，最终门禁重跑通过。临时AST检查改用现有Quartz GFM插件，未新增依赖。

## Recovery

先核对 git diff 和本计划；从未勾选步骤继续，保护用户新修改。用 Node24 命令级 PATH；停止自己开启的临时服务。不调用浏览器。下一篇为 H3-001D/F08 HTTP，先依赖 F05/F07。

## Outcome

内容与非浏览器验收已完成，报告 reports/H3-web-forms.md；16 notes、220 tests、31 HTML/113产物及禁发负面通过。G7和应用交互NOT_RUN。已普通推送a2902097bf18a023db81f7620dec750bab580134，运行34673645597的必需CI/Pages成功，线上HTTP文本冒烟通过；事后证据文档继续自动推送。下一项H3-001D，详见报告。
