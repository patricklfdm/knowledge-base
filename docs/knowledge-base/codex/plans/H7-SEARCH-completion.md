# H7-001 搜索与AI应用连续完成计划

2026-09-13，既有v5，origin=patricklfdm/knowledge-base；HEAD9b15419ee50c1315e3f83f622d3f3e112b1466aa，干净工作区；kb:check90 notes/0 errors。先修H5三项done，H6两条内容线也完成。持续自动普通push授权有效，浏览器仍等H5–H7规划内容完成后统一验收。

## 目标与有限范围

六篇R00–R05，两批各三篇：让读者从合成中文语料构建可解释检索与真实离线评估，再用检索上下文、结构化输出校验及有界重试理解AI应用的工程边界。没有在线AI问答产品、向量库、真实模型调用、付费API、私密数据或其他项目访问；Quartz站内搜索不替换。

新examples/search-lab沿用CPython3.13.0标准库，Node24.21.0/npm11.19.0作CI桥，无新第三方依赖。先实现入口与测试再宣称命令可用。样本、标注、版本摘要、分割、固定参数和无答案项均显式记录，不把微型合成集指标写成生产质量或模型能力。

## 步骤与验证

A：R00中文规范化/分词与倒排，R01词频/长度归一化排序，R02排序评估/无答案/开发与评估分离。维护固定语料与判断集，normal/边界/重复/不可见/故意错误均验证，CI新安装/锁/运行时一致性加负面夹具。

B：R03检索上下文和逐段引用，R04结构化输出与证据校验，R05调用预算、错误分类与检索文本权限边界。抽取式基线与受控假响应只证明本地协议，不称LLM推理或抗注入评测。外部工具执行不加入默认路径。

GATE：六篇第二遍读者任务阅读、全部官方来源在线核对、全部入口/独立故意错误恢复、同SHA质量/Build/Pages与HTTP；关闭父项，下一项H7-002维护机制。浏览器仍NOT_RUN。

## Progress

- [x] A实现、三篇与全套隔离门禁、普通push回执。
- [ ] B实现、三篇与全套隔离门禁、普通push回执。
- [ ] 六篇综合复核、BACKLOG/STATE/报告与最终收尾发布。

## Decisions / Recovery / Outcome

输入仅合成JSON，小规模上限先于处理；简化中文双字词元不是语言学分词器，lexical检索不宣称语义向量。上下文/输出按字符或字节预算，不能冒称token计费或真实网络超时。命令级固定Java/Python，隔离根/tmp/kb-h3b-path.txt受控同步；新例子另建含空格路径与无pip venv，npm ci --offline后运行，独立删守卫必须失败并恢复原字节，再执行根kb:verify/npm test/锁比较。

恢复先核对实际Git/HEAD/origin和工作区保护修改；本计划及每批报告记实际证据，不重复初始化、不改全局环境、不强推、不承诺离线运行。当前A进行中，其余NOT_RUN。

A本地完成：三篇/15 Python/94 notes/378 Node/129HTML，5有效来源和独立错误分母恢复通过，见reports/H7-search-a.md。待普通push同SHA；B实现草稿暂存/tmp/kb-search-create-b.py与-tests-b.py。

A同SHA部署34810326492与HTTP通过；B三篇及31 Python测试初验完成，全套验证进行中。综合来源直连出现403，改为逐URL记录错误，不覆盖本轮web工具已实际读取的官方正文核对；独立例子复核继续。

B本地完成：六篇总复核/31 Python/四入口/97 notes/378 Node/132HTML；五类独立故意错误恢复成功。9官方正文web核对，直接HTTP8个200/JSON Schema403保留，见reports/H7-search-b.md。待普通push B同SHA发布后关闭父项，下一项H7-002。
