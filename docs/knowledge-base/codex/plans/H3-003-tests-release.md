# H3-003：测试、字段演进与运行验收

2026-09-12，基线4ea13cde8291b7cf935b8cf201c4b12599bf7d70；上一F11批已推送，跟踪同SHA部署，不在每项结束停回合。

## 目标与步骤

1. F12A用现有应用归纳单元/HTTP+数据库集成/进程测试，解释失败断言、回归与浏览器NOT_RUN。
2. F12B实作备注note扩展：仅在自动生成临时应用副本中修改页面、字段转换、控制器、API校验、绑定SQL与旧表迁移。基础F11快照保持一致；新增的完整覆盖文件与精确替换器可读可审，匹配失效必须报错，不静默生成残缺例子。
3. 临时旧库有一条合成行程，迁移ADD COLUMN+DEFAULT并登记user_version，重开不重复迁移；事务内故障注入回滚。HTTP/controller创建/修改/非法note无副作用，重启读回；静态页面/脚本检查不冒充DOM运行。
4. F13本机运行/停止/重启、数据库路径、HTTP文本冒烟、Pages与动态应用边界。维护无写入的smoke命令及故意错误端点非零验证；不部署公网应用。
5. 三篇/批，官方来源、两次读者复核、独立含空格环境和整批门禁，报告/台账/STATE、自动push、同SHA部署。完成后继续H3-GATE审计与H4可做的非浏览器准备。

## Progress

- [x] 连续推进方向/先修/来源初核。
- [x] 扩展练习、迁移回滚与冒烟实测。
- [x] 三篇教材、隔离门禁与审阅。
- [x] 发布与H3-GATE/H4衔接。

## 边界与恢复

不在用户数据库迁移，只用脚本新建的临时目录；只读教材来源、无浏览器工具。基础包无新增依赖。字段扩展是受控教学副本，不是通用代码生成器；不支持任意旧库，不声称零停机/备份恢复/生产兼容。H4真实UI需用户明确允许，当前暂停规则继续。

从未勾选步骤与Git差异恢复；Node24命令级PATH、隔离指针同STATE，所有服务由本轮关闭。未完成不标done。

## Outcome

27 notes、259 tests、52 HTML/155产物与独立扩展/迁移/回滚/冒烟通过，见reports/H3-tests-delivery.md；H3-GATE映射完成，见reports/H3-route-gate.md。7e76fca18cfde49422ec2a406aeafbc459cd4f5a已普通推送，同SHA运行34681267525 CI/Pages成功、HTTP文本通过。已接续H4-001A非浏览器工作，不恢复浏览器。
