# Java 主线连续完成计划

2026-09-13，起点f90a3395d809e9fef36b078be8876a8d141fd555，v5/origin/干净工作区与37 notes基础检查通过。用户明确“把java部分整完再停”，连续完成下列三批，不在子项后结束回合；每批验证后自动普通push，同SHA跟踪，浏览器仍延期。固定Microsoft21.0.11+10与Node24.21.0，不重建框架/更换JDK/读取GSE。

## 完成边界

既有J00–J02，加J03–J11共12篇，覆盖H5课程的Java核心语言→组织与I/O→进程/内存→并发→测量学习链。完成是本知识库这条有界Java主线的验收，不宣称涵盖全部Java生态、框架或用户个人掌握；SQL/通用系统、Python、生产专题及H4 UI独立保留，不删已有任务来凑完成。

| 批次 | 单元 | 读者成果与验证 |
| --- | --- | --- |
| H5-001F | J03对象/引用、J04集合/泛型、J05接口/异常 | 构造/封装/方法参数；List/Map/循环和浅复制；实现接口替换策略、异常传播。正常/别名/缺失/编译失败/故障对照 |
| H5-001G | J06文件/资源、J07包/JAR、J08进程/JVM | UTF-8读写与关闭，失败不留半导入；多文件编译打包/独立启动；进程状态/受控堆上限，不伪造GC结论 |
| H5-001H | J09线程/共享状态、J10执行器/取消、J11测量 | 明确交错的丢更新与锁；结果/异常/超时/协作中断；先断言等价再采样，不发布不实性能排序 |
| H5-JAVA-GATE | 路线整体验收 | 先修无环、12篇逐篇来源/例子/练习、独立重现、故意失败、全门禁/同SHA部署、报告与恢复 |

- [x] F：3篇/例子/独立故障/本地门禁/发布。
- [ ] G：3篇/例子/独立故障/本地门禁/发布。
- [ ] H：3篇/例子/独立故障/本地门禁/发布。
- [ ] Java路线综合检查、报告、BACKLOG/STATE、最终推送部署。

## 实施与恢复

examples/java-basics复用临时编译夹具，明确源码清单；测试正常/边界/失败且故障注入只在独立副本。文件、进程和线程仅自建资源；所有子进程超时，线程明确结束，文件清理；不使用sleep碰运气制造竞争。不从Markdown执行任意命令。

每批官方来源核对、自审与另一次读者任务重读，1–3篇逐批发布。依赖未变可同步本会话已干净隔离副本；每批完整kb:verify/npm test/锁/公开过滤，独立包npm ci及负面对照。日志指针失效按维护入口重建。遇实际失败先定位修复，不跳检查、不强推。批次结果追加于本计划及reports/H5-java-*.md；任务状态唯一存BACKLOG。完成本Java门禁才结束本请求。

F实证：22组Java显式测试，299根测试（含辅助加载项），40 notes/66 HTML/183产物；故意copy返回this检出2失败。681bec6同SHA CI/Pages/HTTP通过，报告reports/H5-java-h5f.md。G正在实现，已新增文件/JAR/进程6组真实测试，等待整批隔离门禁。
