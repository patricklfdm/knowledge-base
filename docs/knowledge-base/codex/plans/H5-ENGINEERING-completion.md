# 前端、后端与运行可靠性连续计划

2026-09-13本地，基线11927769407dd5c2b3c9bbd8e19e9956c4d62f0f，v5/origin=patricklfdm/knowledge-base，工作区干净、66 notes基础检查PASS。用户确认按计划进入下一部分；连续三批完成H5-003的有限主线，不逐篇索取确认。自动普通push及浏览器延期不变。

## 目标与范围

从已完成F11–F13应用基础进入E00–E08九篇：React状态/副作用/可访问性和测量，认证授权/版本更新/观测，启停/恢复/故障目标。复用已有SQL、系统、迁移正文，以独立examples/reliable-app承载变化，不改变既有trip-app或Quartz界面。React/ReactDOM官方npm已核验19.3.0，示例独立锁版本，esbuild使用已验证0.27.2；根依赖不升级。

| 部分 | 内容与实际成果 | 故意错误 |
| --- | --- | --- |
| H5-003A | E00组件/props/状态归属，E01异步请求代次/清理，E02语义表单/状态反馈/成本测量 | 状态原地修改、过期结果覆盖、缺label/状态标记；SSR/控制器/构建不是UI验收 |
| H5-003B | E03合成会话与资源授权，E04迁移/乐观并发，E05结构化日志与有界失败 | 越权、失效会话、旧revision覆盖、泄露头/URL、错误当成功 |
| H5-003C | E06就绪/优雅关闭，E07备份新进程恢复与部署证据，E08服务目标/错误预算与恢复综合练习 | 关闭中接受新任务、恢复验证缺数据、请求计数/分母边界 |
| H5-ENGINEERING-GATE | 九篇路线/先修/来源、全部维护入口、完整门禁/发布与台账收尾 | 保留H4 UI/H6/H7未完成状态，不声明生产就绪 |

React客户端可构建，实际渲染终端SSR、纯状态/请求控制器测试；真实浏览器挂载、键盘/辅助技术、Profiler时长与布局统一NOT_RUN，用户已延期。它们不阻止内容门禁，但继续阻止H4/v1.0。合成会话在可信测试入口签发，不实现密码登录/真实账号；不以示例map当生产认证系统。服务器仅127.0.0.1随机端口、数据库与备份仅自身临时目录；不接云/生产/其他项目。

## 验证与恢复

每批先实现例子，再编写来源和可运行声明；作者审阅后另按读者任务核对，故意错误在独立副本运行并恢复。依赖变化在新隔离环境npm ci并检查锁；根受控同步已干净安装隔离目录（/tmp/kb-h3b-path.txt），执行kb:verify/npm test及源码相等检查。Node24.21.0/npm11.19.0、SQLite3.53.4、Python3.13.0、命令级Java21.0.11+10保持。新包安装/锁/测试登记带CI失败夹具。

每批报告、BACKLOG、STATE与计划同步后普通push origin/v5，跟踪同SHA必需quality/Build/Deploy及HTTP文本；不强推、不修改仓库权限、不启动浏览器。临时失效按README重建；恢复先查Git/Actions保护新改动，不重复初始化。

## Progress

- [x] A 前端三篇、锁定例子与CI、完整门禁/发布。
- [x] B 后端三篇、真实请求/DB/失败、完整门禁/发布。
- [ ] C 运行三篇、进程/恢复/预算、完整门禁/发布。
- [ ] 综合验收、H5-003台账与最终同SHA发布。

## Decisions / Outcome

原计划中的生产可靠性指课程和受控实验，不授权公网部署教学后端或付费资源。应用浏览器验收后移，不以SSR测试代替。下一动作H5-003A。每批追加真实结果；完成此有限主线后保存下一项H6-001，不声称离线运行。

H5-003A本地完成：70 notes、342 Node测试、独立例子9项及故意错误恢复PASS；见reports/H5-engineering-a.md。待普通push同SHA发布再继续。

H5-003B本地完成：73 notes、350 Node测试、独立例子17项及故意错误恢复PASS；见reports/H5-engineering-b.md。待普通push同SHA发布再继续。

A同SHA部署和HTTP已通过（ee310d7/34803663548），B完整门禁通过，继续发布后进入C。

H5-003C本地完成：76 notes、357 Node测试、独立例子24项及故意错误恢复PASS；见reports/H5-engineering-c.md。待普通push同SHA发布再继续。

B同SHA部署和HTTP已通过（5419d26/34804170699），C完整门禁通过，继续发布和九篇总验收。
