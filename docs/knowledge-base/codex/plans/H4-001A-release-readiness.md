# H4-001A：发布范围、恢复与集中UI准备

2026-09-12，接续H3-GATE非浏览器验收与第二批推送。此子项只处理不依赖浏览器的可核验准备，H4-001父项与H4-GATE保持未完成。

## 步骤

1. 复核当前公开范围：content仅Markdown，附件尚未准入，非Markdown明确失败；静态产物和索引/过滤负面检查有证据，不以忽略或draft当保密。
2. 编写可恢复发布手册，引用实际命令、v5同SHA门禁/Pages/HTTP证据；在自建临时Git夹具中实跑“通过→引入缺陷→失败→普通revert→恢复通过”，不回退本项目或线上教材。
3. 建立最多3目标的集中UI清单，列具体候选页面、预期、现存搜索问题、有限重试/截图与退出条件；浏览器暂停规则未撤销，未经明确允许不调用。
4. 更新BACKLOG/STATE/报告、普通push文档并跟踪同SHA部署。继续用户允许且依赖满足的工作，不通过把未测UI标done来解锁后续。

## Progress

- [x] H3门禁与前两批证据核对。
- [x] 发布范围复核与临时revert演练，维护脚本加入kb:verify。
- [x] 操作手册、UI候选清单、报告；隔离完整门禁通过。
- [x] 25e7340普通推送，同SHA运行34681756899 CI/Pages成功，HTTP文本通过；检查点已保存。

## 边界

不开启新附件格式，当前全拒绝规则保留；新格式若有实际需求须单独加入允许清单/大小/内容/发布互证，不能把安全策略空缺伪装开放。暂不实现/安装浏览器框架、不改搜索样式。恢复演练只是合成Git夹具，不是生产或数据库恢复证据。H4完整验收仍需实际UI和相关缺陷处理。

## Outcome与恢复

见[验收报告](../../../../reports/H4-release-readiness.md)：259 tests、52 HTML/155产物、负面用例与真实普通revert夹具通过，锁无变化。新增命令在持续集成中随kb:verify执行。教材第二批7e76fca的部署/HTTP已确认；工程批25e7340已完成普通push、同SHA CI/Pages与HTTP文本；按UI清单等待此前暂停边界改变。未许可不连接浏览器，H4父项未完成。中断时先查Git/Actions，成功后核对报告；不要为填自身SHA反复改写提交。

2026-09-12恢复检查：50e2465补记同SHA运行34681850068及HTTP通过。继续H4 UI时打开浏览器被自动审批拒绝（通用“继续”未明确解除此前暂停），未执行/未重试；见reports/H4-ui-authorization.md。H4-001A仍完成，剩余UI任务blocked；下一次只在明确恢复浏览器授权后执行三目标，不重做本计划已完成工作。
