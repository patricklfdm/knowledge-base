# H6-001 分布式基础连续完成计划

2026-09-13，本轮基线c834cfe46d6b70533ef07d58be42228dcf1b8a1b，既有v5/origin=patricklfdm/knowledge-base，工作区干净，76 notes检查通过。H5-001/H5-003先修done；用户要求下一项，继续自动普通push，浏览器等待H5–H7规划内容完成后集中验收。

## 目标与有限范围

两批各三篇D00–D05：解释独立进程之间的不确定结果、重复和恢复，不建设云集群。新增examples/distributed-lab，Node24.21.0原生SQLite3.53.4，无新增第三方依赖。保持已有例子和Quartz不变。

| 批次 | 正文/实现 | 故意错误与证据 |
| --- | --- | --- |
| H6-001A | D00超时与未知结果，D01幂等键/事务账本，D02重试预算/退避 | 模型明确丢请求与丢响应；SQLite同键同意图重放、异意图拒绝、回滚/重开；独立删去幂等检查应失败；受控时钟预算非实测延迟 |
| H6-001B | D03事务发件箱/消费去重，D04复制进度与会话读取，D05租约与fencing | 真实子进程在消费提交后/发送确认前退出并重放；有序复制模型拒绝缺口和落后读取；持久资源端拒绝旧epoch，独立破坏守卫被检出 |
| H6-DISTRIBUTED-GATE | 六篇路线、全部入口/来源、非浏览器门禁与同SHA发布、台账收尾 | 不把模型称多节点集群，不宣称exactly-once/共识或生产可用性 |

无真实账号、邮件、云/付费资源/其他项目。只用合成“便笺创建/事件计数”和自己的临时SQLite/子进程；不启动浏览器。事务账本只约束同一本地数据库，未做跨库原子提交；去重保留期、排序域和恢复假设必须写明。

## 验证与步骤

先实现维护例子与正常/边界/故障断言，再写正文和迁移练习，作者自审后另按读者任务复核。新包在独立含空格目录npm ci，故意错误运行非零后恢复原字节。根受控同步已干净安装隔离目录/tmp/kb-h3b-path.txt，保持固定Java21.0.11+10/Python3.13.0，执行kb:verify/npm test/源码与锁比较。CI新增包安装/锁/登记有故意失败夹具。

每批1–3篇，维护报告/BACKLOG/STATE，普通push origin/v5，跟踪同完整SHA quality/Build/Deploy和HTTP入口/资源/索引/404。收尾只工程文档仍跟踪自身SHA。无强推/PR/tag/权限变化。

## Progress

- [x] A 三篇与维护例子/CI、完整门禁、发布。
- [ ] B 三篇与真实崩溃重放/模型、完整门禁、发布。
- [ ] 综合验收、父任务完成与最终发布；下一项H6-002。

## Recovery / Decisions / Outcome

下一动作H6-001A；发生新事实追加结果。临时目录失效按README重建；恢复先核对Git/HEAD/origin保护已有修改，不重复初始化。不承诺离线持续执行。共识算法/拜占庭故障/真实broker不属于这条基础故障实验主线，数据批流留H6-002。

H6-001A本地完成：80 notes、366 Node测试、独立例子9项及故意错误恢复PASS；见reports/H6-distributed-a.md。待普通push同SHA发布再继续。

H6-001B本地完成：83 notes、375 Node测试、独立例子18项及故意错误恢复PASS；见reports/H6-distributed-b.md。待普通push同SHA发布再继续。

A同SHA34806163219与HTTP已通过；B本地全套门禁83 notes/375 Node测试/114HTML通过，总复核六篇/10官方来源/18项测试/两个demo和故意错误恢复通过，待B发布与台账关闭。
