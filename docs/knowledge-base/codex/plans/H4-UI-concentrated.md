# H4：恢复集中UI验收

2026-09-12，基线365b2890c32018f79f7d07e640be99427ddf095e，工作区干净，origin为patricklfdm/knowledge-base，既有v5。用户明确“恢复浏览器测试”，此前授权阻塞已解除；自动普通push持续有效。

## 目标与步骤

按[三项目标](../../UI_ACCEPTANCE.md)验证1440/390阅读、搜索与键盘、仅合成数据的本机trip-app交互；在真实结果后作必要局部修复，附回归证据。具体页面、断言、最多四张证据图和有限重试见清单。不改框架、整套依赖或部署目标。

- [x] 核对仓库和恢复授权，更新过期暂停说明。
- [x] 尝试电脑控制工具；环境失败后仅重建一次，停止重复尝试。
- [x] 获得可用浏览器页面状态后，实际执行三个目标（2026-09-13恢复后）。
- [x] 依据结果修复、验证，新增6项摘要回归并更新UI门禁；同SHA发布与线上复验已通过。

## 实际发现与恢复

getBrowser初始化没有返回可用页面；getState报Sky Computer Use native pipe startup failed。重置会话一次并重试getBrowser，报CUA_REPL_ENABLED_SURFACES is required。未取得可用页面状态、交互结果或截图，不能以工具调用尝试充当UI验收。

这是当前工具运行环境故障，不是授权问题、站点回归或应用启动失败。未修改用户全局配置、未安装另一套浏览器工具重复尝试，未启动教学服务。本轮环境修复额度已用尽；由运行环境恢复电脑控制能力后再继续本计划的未勾选项，无需重新请求浏览器或普通push授权。

## Outcome（2026-09-12历史）

三项目标NOT_RUN，H4-UI-SITE/H4-UI-APP保持blocked但原因改为工具环境；H4-GATE和后续依赖不变。报告[H4-ui-environment](../../../../reports/H4-ui-environment.md)。本批仅工程记录与授权状态，无运行代码/教材/依赖变化；执行内容检查、台账证据核对、diff检查，按持续授权普通推送并跟踪同SHA CI/Pages。

2026-09-13用户将本计划后移到规划内容完成后，不再以环境恢复作为下一内容任务的前提。历史错误不改写，当前不重试；恢复顺序见BACKLOG。

## 内容完成后的恢复（2026-09-13）

H7-002适用本地验收已完成，H5–H7既定内容依赖满足。按用户的条件安排进入集中三目标，先检查本批部署，再尝试当前工具可用性；历史Sep12失败保留，不视为授权问题。当前恢复的环境失败仍最多一次针对性重建，功能修复最多两轮；不安装另一套工具绕过失败。全部UI仍待实际证据，不能把本地门禁或HTTP结果算通过。

本轮934c8d4dfebaa34a52c5616692b0cc3d821d3dd1已完成同SHA Actions34812504440三项success与HTTP6入口/30资源/索引132/404。当前电脑控制工具恢复，已取得线上首页真实AX和可用tab；Sep12环境阻塞已不再复现，开始三目标交互验收。

首验发现：桌面1440/窄屏390无整页溢出；复制在后台剪贴板为空，可见页面一次重试后原文逐字一致，归为浏览器后台限制。折行inline链接自动点击中心落在空白，键盘Enter实际导航成功，不据此判网站链接坏。窄屏搜索真实复现实体文本和超长偏离命中的摘要；局部修复通过quartz.ts追加只作用于卡片摘要的资源，不改现有索引/排序。原生文本节点与一次实体解码、100代码点命中附近截取有独立测试；随后本机构建浏览器复验。

## 本轮结果（2026-09-13）

三目标已实测，见[UI报告](../../../../reports/H4-ui-acceptance.md)。两轮局部修复处理搜索实体/中文摘要、异步容器观察与高亮，另修取消后的旧提示；复制后台限制在可见页面一次验证成功。未迁移框架/升级依赖。98笔记、395 Node/46检查器、133HTML/318产物，全套隔离与两类独立故意错误恢复通过。教学进程与临时库已清理；6969287a246fe398f1008f4037171533a125fbd0的Actions34815043925三项同SHA成功，线上HTTP6入口/31资源/索引132/404及真实搜索复验通过，H4-GATE已关闭。自建服务/库/站点/标签清理完毕；转入常规维护。工程收尾仍核对自身SHA，不回写自身SHA形成无限提交。
