# H5-001A：行程明细、查询与事务

2026-09-13，基线3f091edf4220a3f9380876f0157436f748185190，v5/目标remote已核对、工作区干净。用户要求先完成规划内容再统一浏览器，继续自动push。

## 目标与范围

两篇连接既有F10/F12知识：一个行程有多条费用明细，解释外键与JOIN、无明细时NULL/COUNT陷阱；再让“新建行程+两条明细”全成或全退，比较缺少事务的半写入。仅合成整数金额，非财务业务/生产迁移。复用无依赖examples/sql-trips，新增独立模块/schema/demo/test，不改变F10已有数据接口。不新增浏览器、框架、API或用户数据库。

## 步骤与验收

- [x] 协调当前授权、台账实际先修、UI后移，保留H4未验收。
- [x] 实现新表/查询/事务与可执行对照；正常、FK/约束、LEFT JOIN计数、半写入/回滚、重开证据。
- [x] 两篇正文与SQL路线导航，官方来源核验，作者自审和另一次读者任务复核。
- [x] 独立含空格包安装/test/demo、故意破坏检出并恢复、隔离完整门禁/锁漂移检查。
- [x] 报告/BACKLOG/STATE、普通push、同SHA CI/Pages/HTTP，继续H5-001B。

## 决策、恢复与边界

SQL是H3已有先修，可先于Java推进H5的一小单元；H5父项仍在建，不因两篇完成就宣称Java/系统全部完成。UI依赖后移来自用户最新要求，不修改CI必需项。金额使用整数分避免本例浮点混淆，不宣传货币完整模型。显式事务只包同步SQLite语句，不跨await、不嵌套；未测断电/并发/重试。所有实验自建临时目录并清理。

中断从未勾选项恢复，先确认Git与临时副本；不触及已有用户修改。内容完成状态与UI状态分离；当前完全不调用浏览器工具。

实际验收见reports/H5-sql-relations-transactions.md：30 notes、266 tests、55 HTML/161产物与独立故障练习通过，原锁无差异；发布后继续H5-001B，不暂停等待UI。

39be2ef已推送，同SHA运行34779959819 CI/Pages与HTTP文本PASS，证据补记报告；H5-001B已实际执行。
