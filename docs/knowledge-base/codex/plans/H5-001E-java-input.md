# H5-001E：Java 值、类型与输入校验

2026-09-13，基线52640bb139bbcb0450ffecc208c87b9b67e27b96，v5/目标origin/干净工作区已核对，基础kb:check35 notes通过。复用Microsoft21.0.11+10临时JDK及Node24.21.0，不改运行时版本/CI依赖/框架。

## 目标与步骤

两篇J01/J02：先区分int/long/double/String/boolean及表达式运算，再把命令行文本按词法、int表示范围、1–30天业务规则分层验证。维护NumericValues与DaysInput独立Java例子，真实javac/java及正常/边界/失败/无副作用测试；无API/数据库/浏览器。

- [x] 例子与测试：整数除法、字符串拼接、先后提升与溢出、整数条件编译失败；解析与业务边界、null、空白/Unicode/符号/小数/超范围、CLI退出与无成功输出。
- [x] 两篇正文、实际来源与迁移练习、作者自审和第二次读者任务复核、路线串联。
- [x] 独立npm ci/test、故意移除上界被检出；干净隔离根门禁/构建/锁与禁发检查。
- [ ] 报告、BACKLOG/STATE、普通push与同SHA CI/Pages/HTTP。

## 决策与边界

输入政策明确只接受ASCII数字串，允许前导零，不自动trim或接受正负号；Integer.parseInt自身接受的格式更宽，不能冒称它只认本例语法。解析得到int仍需1–30限制。纯parse方法失败抛异常，由CLI转换为stderr与非零退出；不把失败变成0或成功记录。long接收已溢出的int不能恢复，必须运算前提升；不扩展成完整金融精度/生产输入安全教程。

复用tools夹具时只添加受维护源码选择和必要多文件编译能力，不能执行Markdown任意代码。所有产物在自建临时目录并清理。新源码/包脚本变动需独立安装，原锁保持；根依赖未变可受控同步已干净隔离副本。恢复先查Git和本计划，再重现日志，不覆盖用户修改。后续对象/集合另设H5-001F，父H5未完成。浏览器NOT_RUN，当前持续自动push授权有效。

本地验收：15组Java、291全量测试、37 notes/63 HTML/177产物通过；移除上界检出3失败，上限14迁移实际通过，锁未变。报告reports/H5-java-input.md，待同SHA发布记录后完成末步。
