# 本批发布记录

用户 2026-09-11 在当前会话明确表示继续并可按需要推送部署。范围为已有 patricklfdm/knowledge-base 的 v5 / GitHub Pages，本批 H0–H2 工程与三篇样板、公开交接文档、H3-001A 两篇及菜单修复。没有 PR/tag/仓库设置/付费资源变更。

## 发布前证据

远端基线 6a4b2eaadb1750e4da9ae872b851b0f5bffe7710 经 git ls-remote 读取，是本地祖先。待推送提交逐批审阅；人工检查新增源码与公开工程资料，常见私钥/GitHub token/AWS key 签名扫描无候选（不是完整秘密检测保证）。未包含用户私密笔记、环境文件、Obsidian 配置或其他项目数据。

验收见 H0-baseline.md、H1-quality-foundation.md、H2-foundations.md、H3-objects-modules.md。最终受测隔离副本与待提交正文/示例/样式逐文件一致。kb:verify PASS：12 notes、34 校验测试、10 示例组、类型检查、24 HTML/99 产物、禁发负面测试；完整 npm test 207 PASS。根/示例锁未漂移；两篇 390/1440 浏览器验收完成。

## 当前状态

首次推送 SHA `bccf231b0ae1ed2c8839589b9fa59c28111dcb2d`；[Publish run 34669410444](https://github.com/patricklfdm/knowledge-base/actions/runs/34669410444) success。quality/verify、Build website、Deploy website 全部 success，部署完成 2026-09-12T03:06:22Z（洛杉矶9月11日20:06）。首次同 SHA 部署成功，但 G8 搜索冒烟发现浏览器仍使用旧索引，修复发布进行中。

线上 HTTP：首页、路线、F03/F04 200；从实际 HTML 解析出的30个本站 CSS/JS 资源全部200；contentIndex 有23项并包含F03/F04；404.html返回200，故意不存在路径返回404。首次冒烟脚本错误猜测无哈希 index.css 得到404，改为读取实际 HTML 哈希资源后通过，并非站点引用断链。

浏览器从线上路线打开F04成功；搜索浅复制却返回无结果，导航仍是旧目录。普通刷新后仍复现；页面声明仍是默认 fetch 稳定 contentIndex URL。直接网络读取索引已新，符合浏览器新鲜缓存沿用旧内容的表现。为索引请求加 cache:"no-cache"，要求每次文档加载时条件验证；SPA 已打开文档仍保持原有内存索引，不声明无需刷新就实时更新。此行为依据 [MDN Request.cache](https://developer.mozilla.org/en-US/docs/Web/API/Request/cache)。无配置选项可控制这条生成请求，限定修改 quartz/components/renderPage.tsx 与对应测试。

缓存修复后完整208测试PASS、kb:verify PASS；在隔离副本暂时撤掉cache选项，新回归确实失败，恢复后renderPage测试通过。最终修复 SHA 与线上搜索结果待下面补录。

## 恢复与回滚

按 origin/v5 最新事实恢复，查相同 head_sha 的 Publish Knowledge Base。失败读取首个实质错误，不跳过门禁、不改部署目标。若需回退，优先用普通 revert 产生新提交并走同门禁；不要 reset 或强推。整批回退的基线是 6a4b2ea，但须先确认远端期间没有他人变更并核对回退范围。

本批发布完成后恢复默认 review-before-push；后续开发任务 F05/F06 的发布需新批授权。
