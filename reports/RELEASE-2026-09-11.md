# 本批发布记录

用户 2026-09-11 在当前会话明确表示继续并可按需要推送部署。范围为已有 patricklfdm/knowledge-base 的 v5 / GitHub Pages，本批 H0–H2 工程与三篇样板、公开交接文档、H3-001A 两篇及菜单修复。没有 PR/tag/仓库设置/付费资源变更。

## 发布前证据

远端基线 6a4b2eaadb1750e4da9ae872b851b0f5bffe7710 经 git ls-remote 读取，是本地祖先。待推送提交逐批审阅；人工检查新增源码与公开工程资料，常见私钥/GitHub token/AWS key 签名扫描无候选（不是完整秘密检测保证）。未包含用户私密笔记、环境文件、Obsidian 配置或其他项目数据。

验收见 H0-baseline.md、H1-quality-foundation.md、H2-foundations.md、H3-objects-modules.md。最终受测隔离副本与待提交正文/示例/样式逐文件一致。kb:verify PASS：12 notes、34 校验测试、10 示例组、类型检查、24 HTML/99 产物、禁发负面测试；完整 npm test 207 PASS。根/示例锁未漂移；两篇 390/1440 浏览器验收完成。

## 当前状态

首次推送 SHA `bccf231b0ae1ed2c8839589b9fa59c28111dcb2d`；[Publish run 34669410444](https://github.com/patricklfdm/knowledge-base/actions/runs/34669410444) success。quality/verify、Build website、Deploy website 全部 success，部署完成 2026-09-12T03:06:22Z（洛杉矶9月11日20:06）。首次同 SHA 部署成功，但 G8 搜索冒烟发现浏览器仍使用旧索引，已由下述补充提交修复并重新发布。

线上 HTTP：首页、路线、F03/F04 200；从实际 HTML 解析出的30个本站 CSS/JS 资源全部200；contentIndex 有23项并包含F03/F04；404.html返回200，故意不存在路径返回404。首次冒烟脚本错误猜测无哈希 index.css 得到404，改为读取实际 HTML 哈希资源后通过，并非站点引用断链。

浏览器从线上路线打开F04成功；搜索浅复制却返回无结果，导航仍是旧目录。普通刷新后仍复现；页面声明仍是默认 fetch 稳定 contentIndex URL。直接网络读取索引已新，符合浏览器新鲜缓存沿用旧内容的表现。为索引请求加 cache:"no-cache"，要求每次文档加载时条件验证；SPA 已打开文档仍保持原有内存索引，不声明无需刷新就实时更新。此行为依据 [MDN Request.cache](https://developer.mozilla.org/en-US/docs/Web/API/Request/cache)。无配置选项可控制这条生成请求，限定修改 quartz/components/renderPage.tsx 与对应测试。

缓存修复后完整208测试PASS、kb:verify PASS；在隔离副本暂时撤掉cache选项，新回归确实失败，恢复后renderPage测试通过。最终修复 SHA `db0210259713b87b48368c1b47b2d18279e81726`；[Publish run 34670174007](https://github.com/patricklfdm/knowledge-base/actions/runs/34670174007) completed/success。同 SHA quality/verify job 103489937564、Build website 103490005301、Deploy website 103490068500 全部success，完成时间分别为2026-09-12T03:22:41Z、03:23:11Z、03:23:59Z（部署为洛杉矶9月11日20:23:59）。上游 Build and Test/Docker/v5 Preview 按仓库条件 skipped，不是本站必需门禁。

最终 G8 PASS：在此前搜索失败的同一 Chrome 会话重新加载 F04，DOM 确认生成请求带 cache:no-cache；搜索“浅复制”命中 F03、Enter 后 URL/标题正确；搜索 createTrip 命中 F04、Enter 后 URL/标题正确，导航也包含新文章。线上正文截图复核无整页横向溢出（实际1450px视口/scrollWidth1450）。发布地址 https://patricklfdm.github.io/knowledge-base/ 。

代码与正文已发布至 db02102；这份事后验收报告与最终台账作为单独本地文档提交保存，不为记录自身 SHA 再触发无内容变化的部署。临时 localhost 预览已停止（该进程退出143），测试标签关闭，视口恢复。下一次可将这份报告随下一批正常推送。

## 遗留限制

搜索摘要实体与中文片段质量留在 H4-001；本次解决索引更新，不改变摘要算法。已打开的 SPA 页面仍需重新加载以读取新部署内容。远程字体网络依赖、缺失但未使用的 Excalidraw 警告、上游全仓格式差异仍见基线报告；没有放宽门禁。首条应用路线尚未完成，不宣称v1.0全部完成。

## 恢复与回滚

按 origin/v5 最新事实恢复，查相同 head_sha 的 Publish Knowledge Base。失败读取首个实质错误，不跳过门禁、不改部署目标。若需回退，优先用普通 revert 产生新提交并走同门禁；不要 reset 或强推。整批回退的基线是 6a4b2ea，但须先确认远端期间没有他人变更并核对回退范围。

本批发布完成后恢复默认 review-before-push；后续开发任务 F05/F06 的发布需新批授权。
