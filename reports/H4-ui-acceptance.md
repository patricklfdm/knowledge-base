# H4 集中浏览器验收

2026-09-13（UTC09-14），基线934c8d4dfebaa34a52c5616692b0cc3d821d3dd1，patricklfdm/knowledge-base既有v5。H5–H7既定内容完成后按[三目标计划](../docs/knowledge-base/UI_ACCEPTANCE.md)执行；自动普通push继续。Sep12工具环境失败保留为历史，本轮已恢复可用，不能据历史失败继续标阻塞。

## 真实浏览器结果

使用Codex In-app Browser，工具未报告引擎与完整版本。站点先验线上基线，再在同源带/knowledge-base/路径的本机正式构建副本复验修复；教学应用仅用本轮临时数据库和合成数据。

1. **阅读导航。** 线上首页→全栈路线→F11，返回路线→F12；1440与390视口中实际文档宽等于视口，没有整页横向溢出。窄屏展开/收起目录后文章可读。F11复制代码与四行原文逐字一致；第一次后台页面剪贴板为空，显示页面后一次针对性重试成功，未修改复制组件。折行inline链接的自动点击中心落在换行空白，改用Enter实际成功，未把工具点击几何问题归为网站坏链接。本机副本抽查首页反馈、数据与搜索路线，390宽导航/标题/表格可读。
2. **搜索与键盘。** 请求校验、SQLite、备注有目标文章；上下键改变选中项，Enter打开持久化API，Escape关闭，SPA跳转后重开搜索正常。原窄屏卡片实际显示`&quot;`/`&lt;`且摘要长度1260/1312等，命中可能不在前三行。局部修复后摘要最多102代码点（100正文+两端省略号），完整字面命中距开头最多21代码点；请求校验在路线摘要实际高亮，SQLite/备注也验证。INVALID_TRIP搜索中的JSON引号显示为`"`，卡片没有生成img/b/script元素。没有完整字面命中时显示开头，不假造高亮，不声称改善排序相关性。
3. **教学应用。** 浏览器键盘创建`<b>海湾</b>`/3天，列表只有一行且没有b元素；选择编辑31天显示“天数必须是1到30的整数”，原记录仍3。取消清空草稿、回到新增；发现旧错误提示残留后局部修复，复验提示“已清空表单，可新增行程”，焦点回到目的地。合法修改5天时，将已核对的本轮服务进程暂停20秒并自动恢复：浏览器显示“正在保存…”、aria-busy=true、全部6个控件禁用；恢复后成功提示、所有控件可用、同一条记录5天。正常关闭旧进程、新进程打开同一临时库，浏览器仍读到这一条5天记录。

## 局部修改与边界

quartz.ts通过现有transformer资源入口加载自有摘要脚本；保留上游搜索组件、索引、排序、预览、链接和键盘逻辑，不修改node_modules、不增加依赖。纯函数只解码Quartz产生的五种实体一次，再按代码点截取；DOM使用text node与span.textContent，不用innerHTML插入解码结果。

第一轮修复在纯函数测试通过后，真实页面发现结果容器由上游异步创建，初次未被观察。第二轮改为观察静态search-layout父容器，并在同轮收尾调整高亮颜色与取消反馈。观察器在nav/render时断开旧实例，WeakMap避免自身修改造成循环；SPA后实测正常。初始测试还修正了期望字符串漏引号，以及短文本不应从中间截断的边界。

这不是完整网站检索引擎替换、模糊相关性评估、全浏览器认证或自动浏览器CI。真实屏幕阅读器、其他浏览器/设备、F07独立页面、F12生成的备注变体与React实验、保存成功但列表刷新失败的浏览器故障分支均NOT_RUN；对应既有非浏览器测试不改称UI。有限三目标覆盖本站发布阅读与F11基础应用；原课程未测边界保留。两篇F11仅补实际UI证据和tested_with，verified_on不刷新。

## 本地与发布门禁

新摘要纯函数6项正反测试已通过（转义一次/保留字面实体、长中文/晚命中、大小写/正则字符、无命中/空输入、Unicode、HTML样式文字），tsc通过。直接tsx CLI第一次因沙箱IPC监听EPERM未启动测试，改为Node --import tsx执行同一套测试；完整隔离门禁使用正常环境审批，不把IPC失败当测试通过。

最终隔离kb:verify/npm test通过：98笔记、46检查器/395 Node测试（45 suites）、Python29+33+31，133HTML/318产物；完整示例、回退演练、tsc、产物/base path/发布正反控制均通过。独立将实体解码改为原样返回、将命中截取起点改为0，原断言均以AssertionError和退出1检出，逐一恢复后6项通过。维护源码与隔离源码字节一致、锁无漂移；日志/tmp/ui-verify.log、ui-tests.log、ui-mutant-decode.log、ui-mutant-position.log、ui-restored.log。最终浏览器高亮为rgb(235,235,236)、透明背景和下划线，实际可读；1440桌面备注搜索8结果、无控制台错误。本批同SHA发布已通过，具体回执见下文。原H7提交Actions34812504440同SHA三项success、HTTP6入口/30资源/索引132/404通过，不能代替本次UI修复提交。

## 恢复与清理

隔离根/tmp/kb-h3b-path.txt；本站临时产物/tmp/kb-ui-site；应用临时目录/tmp/kb-ui-app-dir.txt。服务只绑定127.0.0.1，验证结束正常停止、确认无残留后删除自己创建的临时应用/数据库；恢复浏览器视口与后台显示，不操作用户已有页面或资料。下一动作是完成待补门禁、更新BACKLOG/STATE并普通push自身SHA，禁止强推或冒称离线持续运行。

本轮教学服务已正常停止，自建应用/数据库目录已删除。本站临时浏览器与静态服务在最终线上复验后清理；最终视口与后台显示已恢复。自动测试与手工浏览器集中流程共同构成维护入口，未声明不存在的kb:e2e或远端浏览器CI。

## 同SHA发布与H4综合验收

修复提交6969287a246fe398f1008f4037171533a125fbd0已普通push，Actions34815043925同SHA quality / verify、Build website、Deploy website分别于2026-09-14T06:51:35Z、06:52:15Z、06:52:27Z成功。线上HTTP6入口/31资源200、索引132含目标、缺页404通过，日志/tmp/ui-http.log。真实线上390视口复查INVALID_TRIP与备注：正常引号、102代码点上限、清晰高亮和正确目标成立，控制台无错误。首次重载后过早激活搜索时输入尚未出现，读取当前闭合状态后再打开成功；不将初始化等待记为摘要修复失败，也未增加源码修复轮次。

所有本轮临时服务/数据库/站点副本与三个自建标签已清理，视口override重置、显示恢复后台。H4-001A既有发布/附件拒绝和真实回退夹具继续通过，H4-001与两个集中UI任务据本报告完成，H4-GATE据本次完整本地门禁、同SHA发布和线上UI复验关闭。v1.0验收限于既定三目标与支持矩阵，不代表全浏览器、全部教学变体、辅助技术、生产运行或用户个人掌握。

H0–H7既定任务台账已无未完成项；后续按kb:review、实际反馈与明确的新需求维护，不为了继续执行自动扩张课程。工程文档收尾仍普通push并核对其实际HEAD自身同SHA结果；上述修复SHA不冒充收尾HEAD，不反复amend只为把自身SHA写入自身文件。
