# H5-001A：SQL关系查询与事务验收

2026-09-13，基线3f091edf4220a3f9380876f0157436f748185190，patricklfdm/knowledge-base v5，接手工作区干净。按用户最新要求浏览器后移到规划内容完成后，内容按实际先修继续；持续自动普通push保持。

## 交付

新增S01 joins-and-null、S02 transactions-and-rollback两篇与SQL支线导航；首页/知识地图/F13接上真实入口。30 notes为8导航+22教材，不把导航算教材。无站点表现层修改。

复用examples/sql-trips无依赖环境，新增独立ledger表/模块/demo/test；原F10 schema/store与接口保持。两表主外键、正整数费用、JOIN/LEFT JOIN/COUNT/ON-WHERE；显式事务和无显式事务的半写入对照。费用是合成整数分，不操作真实账户/账目/用户数据库，不部署API。

台账保留H4未验收和H5父项在建。H5内容依赖改为H3-GATE+H4-001A，H7维护依赖H4-001A；H6/H7知识先修未撤销。UI任务等待既有H5–H7内容里程碑，调整依据2026-09-13用户要求，不是把环境问题伪装PASS。参考文件的首路线优先范围已由当前后续安排覆盖，每批仍有限边界。

## 实测

Node24.21.0/npm11.19.0，内置SQLite3.53.4，macOS arm64。独立含空格副本npm ci/test/ledger通过，原8+新7共15组；锁不漂移。故意把ROLLBACK改COMMIT，7组新测试中3组失败且退出1，恢复后逐文件匹配原源。另实跑练习[100,200,-1]：无事务保留两笔300，有事务为空；筛选200得到1/1/0，COUNT星号对零明细错误计1。维护测试证明FK拒绝孤儿及被引用父行删除、约束、不同失败位置保留既有数据、事务失败后连接可继续、嵌套BEGIN拒绝且不撤销调用方事务、同文件重开提交结果保留。

受控同步到本会话已干净安装的隔离副本，kb:verify与npm test通过：30 notes、34检查器、266 tests/45 suites，0 fail/skip；全示例、recovery夹具、tsc、正式构建、55 HTML/161产物、公开对照/3禁发marker与注入泄漏检出PASS。无依赖变化，原有锁文件无差异。日志/tmp/h5a-verify.log、/tmp/h5a-tests.log、/tmp/kb-h5a-mutant.log；独立指针/tmp/kb-h5a-example-path.txt。临时日志失效可由维护命令重现。

## 编辑与边界

官方文本核对SQLite SELECT、Foreign Keys、聚合函数、COALESCE、事务与冲突处理，正文就近链接。作者自审后另按读者任务独立重读：先预测空明细与条件位置，再比较故障位置、原有数据与提交结果；修正叙述避免把一行配对当一行行程、catch当自动回滚、BEGIN IMMEDIATE当无等待保证。未冒称独立专家。SQL摘录标明完整源码位置；ON/WHERE、COUNT和半写入均有真实反例。

浏览器G7与真实DOM仍NOT_RUN：用户批准内容建设完成后集中验收。新增ledger正常重开是同进程新连接，F10已有新进程证据单列；本批没有并发、断电、磁盘故障、生产恢复、完整货币模型或外部副作用回滚证明。全部实验使用自建内存/临时文件并清理，无监听残留；历史UI/搜索/Excalidraw/字体与上游格式问题保留。

## 发布与下一项

本批适用门禁完成，按持续授权普通push并跟踪同SHA CI/Pages/HTTP；发布实证在下一批补记，不为自身SHA反复改写。本报告以基线加差异定位。下一项H5-001B：一个有限索引与查询计划单元，继续内容，不恢复浏览器；Java/系统等父项内容仍待逐批实现。
